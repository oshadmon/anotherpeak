Network:

Todo:
1. setup network - 2 machines
    1 - Standlaone w/ Query
    1 - operator
2. create + publish mapping policy
3. setup aggregation script(s)
    * store raw data
    * for aggregation specifying logical table name for aggregated storage
    * show aggregation for all columns
    * store aggregation for only 3 of the columns
    * can we store only a subset of the raw data?
4. attempt to insert data via REST POST
